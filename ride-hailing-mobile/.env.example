# 📱 Ride Hailing Mobile App - Environment Configuration

# =============================================================================
# BACKEND API CONFIGURATION
# =============================================================================
# Backend API base URL (REQUIRED)
# For local development with tunnel mode:
EXPO_PUBLIC_API_URL=http://localhost:3000/api

# For local network testing (replace with your computer's IP):
# EXPO_PUBLIC_API_URL=http://192.168.1.100:3000/api

# For production:
# EXPO_PUBLIC_API_URL=https://your-api-domain.com/api

# WebSocket URL for real-time features
EXPO_PUBLIC_WEBSOCKET_URL=ws://localhost:3000

# =============================================================================
# GOOGLE MAPS CONFIGURATION (REQUIRED)
# =============================================================================
# Get your API key from Google Cloud Console
# Enable: Maps SDK for Android, Maps SDK for iOS, Directions API, Places API
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# =============================================================================
# AUTHENTICATION CONFIGURATION
# =============================================================================
# JWT Secret (should match backend - for token validation)
EXPO_PUBLIC_JWT_SECRET=your-jwt-secret

# =============================================================================
# PAYMENT INTEGRATION (OPTIONAL)
# =============================================================================
# Razorpay
EXPO_PUBLIC_RAZORPAY_KEY_ID=your-razorpay-key-id

# Stripe
EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=your-stripe-publishable-key

# =============================================================================
# EXTERNAL SERVICES (OPTIONAL)
# =============================================================================
# Supabase (if using for additional services)
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# =============================================================================
# APP CONFIGURATION
# =============================================================================
# App environment
EXPO_PUBLIC_APP_ENV=development

# Enable debug features
EXPO_PUBLIC_DEBUG_MODE=true

# API request timeout (milliseconds)
EXPO_PUBLIC_API_TIMEOUT=10000

# =============================================================================
# FEATURE FLAGS
# =============================================================================
# Enable/disable features in the app
EXPO_PUBLIC_ENABLE_DRIVER_MODE=true
EXPO_PUBLIC_ENABLE_PAYMENTS=true
EXPO_PUBLIC_ENABLE_RATINGS=true
EXPO_PUBLIC_ENABLE_CHAT=false

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================
# Mock services for development
EXPO_PUBLIC_MOCK_LOCATION=false
EXPO_PUBLIC_MOCK_PAYMENTS=true

# =============================================================================
# ANALYTICS & MONITORING (OPTIONAL)
# =============================================================================
# Google Analytics
EXPO_PUBLIC_GA_TRACKING_ID=your-ga-tracking-id

# Sentry for error tracking
EXPO_PUBLIC_SENTRY_DSN=your-sentry-dsn

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================
# For different testing scenarios:

# Option 1: Tunnel Mode (Recommended for mobile testing)
# EXPO_PUBLIC_API_URL=http://localhost:3000/api
# Start with: npx expo start --tunnel

# Option 2: Local Network (for same WiFi testing)
# EXPO_PUBLIC_API_URL=http://YOUR_COMPUTER_IP:3000/api
# Find your IP: ifconfig (Mac/Linux) or ipconfig (Windows)

# Option 3: Production/Staging
# EXPO_PUBLIC_API_URL=https://api.yourdomain.com/api

# =============================================================================
# INSTRUCTIONS
# =============================================================================
# 1. Copy this file to .env: cp .env.example .env
# 2. Update EXPO_PUBLIC_API_URL with your backend URL
# 3. Get Google Maps API key and update EXPO_PUBLIC_GOOGLE_MAPS_API_KEY
# 4. For mobile testing, use tunnel mode: npx expo start --tunnel
# 5. For production, use HTTPS URLs and remove debug flags
# 6. All EXPO_PUBLIC_ variables are available in the app code
# 7. Never commit .env file to version control

# =============================================================================
# GETTING API KEYS
# =============================================================================
# Google Maps API:
# 1. Go to Google Cloud Console (console.cloud.google.com)
# 2. Create project or select existing
# 3. Enable APIs: Maps SDK for Android, Maps SDK for iOS, Directions API, Places API
# 4. Create credentials > API Key
# 5. Restrict key to your app package name for security

# Razorpay (for payments):
# 1. Sign up at razorpay.com
# 2. Get API keys from dashboard
# 3. Use test keys for development

# =============================================================================
# TESTING CONFIGURATIONS
# =============================================================================
# For local testing:
# EXPO_PUBLIC_API_URL=http://localhost:3000/api

# For device testing on same network:
# EXPO_PUBLIC_API_URL=http://192.168.1.100:3000/api

# For tunnel testing (works anywhere):
# EXPO_PUBLIC_API_URL=http://localhost:3000/api
# npx expo start --tunnel